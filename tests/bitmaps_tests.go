package roar_test

import (
	roar "roar/types"
	"testing"
)

var bitmap_0 roar.Bitmaps

func InsertElemsIntoBitmap(t *testing.T) {
	bitmap_0 = roar.CreateBitmap()
	t.Run("Insert a few elements into bitmap", func(t *testing.T) {
		t.Error("Test not implemented")
	})
}
